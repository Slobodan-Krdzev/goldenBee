# Optional: Override API base URL. When unset:
# - Local (localhost/127.0.0.1): uses http://localhost:3001
# - Hosted (Vercel): uses same-origin /api (proxy uses BACKEND_URL)
VITE_API_URL=http://localhost:3001
